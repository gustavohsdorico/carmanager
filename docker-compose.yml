  version: '3'

  services:
    db:
      image: postgres
      environment:
        - POSTGRES_DB=carmanager
        - POSTGRES_USER=fsengine
        - POSTGRES_PASSWORD=fsengine
    web:
      build: .
      command: python manage.py runserver 0.0.0.0:8000
      volumes:
        - .:/code
      ports:
        - "8000:8000"
      depends_on:
        - db